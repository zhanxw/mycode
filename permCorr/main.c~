#include <stio.h>
#include <stlib.h>


// shuffle an array using Knuth's method
#define SHUFFLE_T
extern shuffle(SHUFFLE_T* data, int n);


int main(int argc, char *argv[])
{
    
    return 0;
}
